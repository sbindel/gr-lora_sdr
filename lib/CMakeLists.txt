# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-lora_sdr
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND lora_sdr_sources
    whitening_impl.cc
    add_crc_impl.cc
    crc_verif_impl.cc
    data_source_impl.cc
    deinterleaver_impl.cc
    dewhitening_impl.cc
    err_measures_impl.cc
    fft_demod_impl.cc
    kiss_fft.c
    frame_src_impl.cc
    frame_sync_impl.cc
    gray_decode_impl.cc
    gray_enc_impl.cc
    hamming_dec_impl.cc
    hamming_enc_impl.cc
    header_decoder_impl.cc
    header_impl.cc
    interleaver_impl.cc
    modulate_impl.cc
    mu_demod_impl.cc
    mu_detection_impl.cc
    mu_synchro_impl.cc
    noise_est_impl.cc
    partial_ml_impl.cc
    RH_RF95_header_impl.cc
    signal_detector_impl.cc
)

set(lora_sdr_sources "${lora_sdr_sources}" PARENT_SCOPE)
if(NOT lora_sdr_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT lora_sdr_sources)

add_library(gnuradio-lora_sdr SHARED ${lora_sdr_sources})
target_link_libraries(gnuradio-lora_sdr gnuradio::gnuradio-runtime Volk::volk)
target_include_directories(gnuradio-lora_sdr
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-lora_sdr PROPERTIES DEFINE_SYMBOL "gnuradio_lora_sdr_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-lora_sdr PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-lora_sdr)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_lora_sdr_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-lora_sdr)

if(NOT test_lora_sdr_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_lora_sdr_sources)

foreach(qa_file ${test_lora_sdr_sources})
    GR_ADD_CPP_TEST("lora_sdr_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
